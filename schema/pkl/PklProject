amends "pkl:Project"

// Package configuration for local development.
// Update baseUri and packageZipUrl when publishing to a registry.
package {
  name = "tailscale"
  baseUri = "package://localhost/plugins/tailscale/schema/pkl/tailscale"
  version = "0.1.0"
  packageZipUrl = "https://localhost/plugins/tailscale/schema/pkl/tailscale@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.86.1"
  }
}
